# Shared libraries

Shared configuration and board pins, battery estimation and owner-reset helpers. Local secrets belong in ignored `AppSecrets.h`. Product-specific source belongs under each product firmware directory.
