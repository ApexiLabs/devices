# Shared protocols

Single source for Logger/Dash BLE UUIDs, bounded frames, sensor fault values and system events. Both products include these headers directly. Any incompatible protocol change requires explicit versioning and compatibility tests.
